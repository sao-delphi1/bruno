info:
  name: update_password
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{API_URL}}/resetpass"
  body:
    type: json
    data: |-
      {
          "userid" : "shoen",
          "password" : "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
