meta {
  name: Change Pass
  type: http
  seq: 3
}

patch {
  url: {{API_URL}}/changepass
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
   {
       "userid" : "admin",
       "oldpassword" : "",
       "newpassword" : ""
   }
  
  
}
