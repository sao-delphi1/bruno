meta {
  name: create user
  type: http
  seq: 3
}

post {
  url: {{API_URL}}/user
  body: json
  auth: inherit
}

body:json {
  {
      "userid": "Test",
      "password": "123",
      "kdgroup": "SALES"
  }
}

settings {
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
}
