meta {
  name: delete user
  type: http
  seq: 4
}

delete {
  url: {{API_URL}}/user
  body: json
  auth: inherit
}

body:json {
  {
      "userid" : "Test"
  }
}

settings {
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
}
